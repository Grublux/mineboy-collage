[profile.default]
solc_version = "0.8.24"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = false
bytecode_hash = "none"

# Exclude legacy contracts from compilation
exclude = ["contracts/legacy/**", "contracts/legacy_backup/**", "test/legacy/**", "test/legacy_backup/**"]

[profile.ci]
solc_version = "0.8.24"
evm_version = "cancun"
optimizer = true
optimizer_runs = 800
via_ir = false
bytecode_hash = "none"
exclude = ["contracts/legacy/**", "contracts/legacy_backup/**", "test/legacy/**", "test/legacy_backup/**"]


